
.container
  - if @tags.any?
    %table.table.table-hover
      %tr
        %td
          %strong
            Nom du tag
        %td
          %strong
            Voir
        %td
          %strong
            Modifier
        %td
          %strong
            Supprimer

      - @tags.each do |t|
        %tr
          %td
            = t.name
          %td
            = link_to "Voir", tag_path(t), class: 'btn btn-primary'
          %td
            = link_to "Editer", edit_tag_path(t), class: 'btn btn-warning'
          %td
            = link_to "Supprimer", tag_path(t), method: :delete, class: 'btn btn-danger'
  -else
    %p Pas de tag de trouver
